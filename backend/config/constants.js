const APP_NAME = "VRR Events";
const API_PREFIX = "/api/v1";
const DEFAULT_PORT = 5000;
const NODE_ENV = process.env.NODE_ENV || "development";

module.exports = {
  APP_NAME,
  API_PREFIX,
  DEFAULT_PORT,
  NODE_ENV,
};
